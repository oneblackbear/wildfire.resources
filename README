RESOURCES REQUIRES:
- wildfire v7
- meta plugin

TODO:
  USERS:
  - password reset page
  - user profile page
  REPORTS:
  INVOICING:
  COMMENTS:
  MISC:
  - replace the join classes with ones that filters on the logged in group - these are partly done but seem to break many things, big debug job!
  - header search
  - a sites listing to attach to clients (domains, servers, analytics logins)
  - an analytics module based on the sites for clients to see overview figures
  - ordering of things to be A-Z (part of join classes)



